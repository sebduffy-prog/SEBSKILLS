# SEBSKILLS — full catalogue

_Auto-generated by `scripts/build_manifest.py` — **436 skills** across **31 categories**. Do not edit by hand._

Type `/sebduffy <what you want>` to route to any of these automatically, or `/sebduffy <category>` to browse one.


## 3d · 8

- **`blender-mcp-asset-import`** — Pull real and AI-generated assets into a live Blender session over ahujasid/blender-mcp — Poly Haven HDRIs / PBR textures / models, Sketchfa
- **`blender-mcp-bpy-api-navigator`** — Write correct bpy the FIRST time instead of guessing — resolve the exact operator argument names, property paths, and enum string values aga
- **`blender-mcp-procedural-generation`** — Generate PARAMETRIC content in a live Blender session over MCP — build Geometry Nodes trees, stack modifiers (Array / Subsurf / Bevel / Soli
- **`blender-mcp-render-review-loop`** — Close the visual feedback loop in a live Blender + MCP session: render (or screenshot the viewport), Read the resulting image back, critique
- **`blender-mcp-scene-building`** — Build and modify a Blender scene over MCP with CORRECT bpy — add primitives, set transforms/parenting, author Principled-BSDF materials, pla
- **`blender-mcp-scene-inspection`** — Read a Blender scene BEFORE editing it — collection/object hierarchy, per-object detail, data-block counts, render engine, missing external
- **`blender-mcp-setup`** — Install and wire up the ahujasid/blender-mcp add-on so Claude can drive Blender end-to-end
- **`photo-to-3d-asset`** — Turn a single product/hero photo (or a text prompt) into a clean, textured, game/AR-ready 3D mesh (GLB) using open image-to-3D models — Huny

## adtech-ops · 1

- **`paid-media-campaign-ops`** — Run the media-agency day-job from the terminal: create, list, pause, budget and report on Meta (Facebook/Instagram) ad campaigns with the of

## agent-frameworks · 23

- **`a2a-agent-interop`** — Make agents from different frameworks or vendors talk to each other over Google's Agent2Agent (A2A) protocol — publish a discoverable Agent
- **`agent-evals-and-tracing`** — Evaluate and trace agentic LLM systems end to end — score whole trajectories and tool-call accuracy, choose deterministic (final-state / exa
- **`agent-orchestration-patterns`** — Framework-agnostic reference and decision heuristics for the canonical multi-agent topologies — prompt chaining, routing/classifier, paralle
- **`baml-structured-prompts`** — Author LLM functions in BAML (BoundaryML) — a type-safe DSL where input/output classes, enums, and jinja prompt templates compile to a typed
- **`classifier-agent-routing`** — Route every user turn to the best specialist agent with a central intent Classifier that keeps a global view of the whole conversation — usi
- **`claude-tag-slack-agent`** — Design and operate Claude Tag — Anthropic's shared @Claude teammate inside Slack channels (an evolution of Claude Code)
- **`computer-use-agent`** — Scaffold a sandboxed computer/browser-driving agent that operates a real desktop or browser like a human — screenshot then plan then click/t
- **`crewai-flows-orchestration`** — Build multi-agent systems with CrewAI — role-based Crews (Agent/Task, sequential or hierarchical process, YAML+Pydantic config via @CrewBase
- **`dspy-program-optimization`** — Build and optimize LLM programs with DSPy instead of hand-tuning prompt strings
- **`dynamic-workflow-authoring`** — Author, run, and save Claude Code dynamic-workflow scripts that move the plan out of the context window into JavaScript and fan out dozens-t
- **`handoff-router-swarm`** — Build handoff-based agent swarms where specialists transfer control directly to each other — no central supervisor
- **`human-in-the-loop-approval`** — Put a durable human approval gate in front of any consequential agent action — send email, post to Slack, spend money, deploy, delete, DM a
- **`instructor-structured-outputs`** — Get reliable, typed data out of ANY LLM by declaring a Pydantic model as response_model — Instructor patches the provider client so a bad/in
- **`langgraph-durable-workflows`** — Build stateful, durable, multi-agent systems with LangGraph — StateGraph with typed state and reducers, supervisor and orchestrator-worker t
- **`llm-guardrails-injection-defense`** — Add a standalone, provider-agnostic guardrail layer that defends ANY LLM app against prompt injection, jailbreaks, and unsafe output — input
- **`llm-observability`** — Wire full production LLM/agent observability into an existing Claude or OpenAI app with Langfuse — trace every call and agent trajectory (in
- **`longhorizon-research-agent`** — Build a DeerFlow-style long-horizon "super-agent" harness that takes a deep multi-step brief and runs it to a finished deliverable — a lead
- **`n8n-ai-workflow-automation`** — Build low-code visual AI agents and automations in n8n — 400+ integration nodes plus the LangChain AI nodes (AI Agent, Chat Trigger, chat mo
- **`openai-agents-sdk`** — Build multi-agent apps with the OpenAI Agents SDK (the production successor to Swarm) — define Agents with typed tools, wire Handoffs that t
- **`prompt-optimization`** — Harden a prompt the eval-driven way: build a tiny labelled eval set, score the current prompt to get a baseline, diagnose the actual failure
- **`pydantic-ai-typed-agents`** — Build type-safe Python LLM agents with PydanticAI — a typed deps_type for dependency injection via RunContext, a Pydantic output_type for st
- **`swarm-evaluation-harness`** — Evaluate multi-agent swarms at the TRAJECTORY level, not just the final answer — score handoff/routing correctness, per-agent sub-goal (task
- **`swarm-guardrails`** — Wrap an LLM app or agent with OpenAI Guardrails so a tripwire HALTS the run before tokens or side-effects are spent — jailbreak blocking, mo

## agent-simulation · 4

- **`agentsociety-urban-experiment`** — Design and run controlled treatment/control experiments on large LLM-agent populations in AgentSociety's urban simulator — inject interventi
- **`generative-agent-architecture`** — Give an LLM agent believable long-run behaviour with Stanford's "Generative Agents" (Smallville) cognitive loop: a memory stream retrieved b
- **`message-diffusion-network-sim`** — Forecast how a campaign line, claim, or headline SPREADS or DIES across a synthetic social graph of Claude personas — not by polling isolate
- **`oasis-social-media-simulation`** — Run agent-based social-media simulations with OASIS (camel-ai/oasis) to model virality, polarization, rumor/misinformation spread, and audie

## building-agents · 17

- **`adversarial-skill-forge`** — Harden a Claude skill through generator-vs-adversary self-play over skill-creator's eval harness
- **`agent-code-sandbox`** — Give a code-writing agent a secure, ephemeral runtime so LLM-generated code never executes on your host
- **`auto-dream-loop`** — Give an agent an idle-time "sleep and dream" self-improvement loop that only ships measured wins
- **`autosuggestive-schema-builder`** — Build Lovable.dev / v0-style no-code builders where Claude proposes accept/reject changes against a declarative content schema with a live p
- **`claude-advanced-tool-use`** — Cut multi-tool / multi-MCP agent overhead by wiring Anthropic's Tool Search Tool plus `defer_loading` and `input_examples` into an SDK app
- **`claude-api`** — Build, debug, and optimize Claude API / Anthropic SDK apps
- **`claude-code-agent-teams`** — Set up and drive Claude Code agent teams end-to-end: enable the experimental flag, spawn reusable teammate roles from subagent definitions
- **`claude-programmatic-tool-calling`** — Let Claude orchestrate your tools from Python inside the code-execution sandbox instead of one model round-trip per call
- **`claude-structured-outputs`** — Force Claude to return JSON that provably matches your schema — the native Claude answer to instructor/BAML
- **`federated-knowledge-memory`** — Build a shared, append-only, provenance-tracked knowledge store for a whole skill library or agent fleet — any contributor's LLM (any provid
- **`managed-agents-outcomes`** — Turn eval from an offline afterthought into a per-session go/no-go by wiring Anthropic Managed Agents' rubric-based Outcomes as an in-loop d
- **`mcp-builder`** — Guide for creating high-quality MCP (Model Context Protocol) servers that enable LLMs to interact with external services through well-design
- **`moltbook`** — Run N heterogeneous agents (different models/personas) through structured propose->critique->revise rounds where EVERY proposed change must
- **`openclaw-personal-agent-harness`** — Stand up and HARDEN a self-hosted OpenClaw personal-agent gateway (Steinberger's open-source assistant, formerly Clawdbot/Moltbot) that give
- **`permanent-agent`** — Stand up an always-on, self-maintaining agent that outlives a single session
- **`skill-creator`** — Create, edit, verify, and optimize skills (SKILL.md files)
- **`unattended-longrun-envelope`** — Pre-flight and safety-wrap any hours-to-days unattended Claude Code run — a dynamic workflow (ultracode), an agent team, or a scheduled clou

## commerce · 2

- **`agentic-commerce-integration`** — Wire a merchant catalog into ChatGPT Instant Checkout via the Agentic Commerce Protocol (ACP)
- **`product-feed-retail-media`** — Generate, optimise and debug product-catalog feeds for Google Merchant Center and Meta (Facebook/Instagram) commerce, plus SKU-level retail-

## compliance · 2

- **`ad-claims-substantiation-gate`** — Port Claude-for-Legal's citation-gate to advertising
- **`consent-privacy-compliance`** — Audit and fix digital-advertising privacy compliance before non-consented tags stop serving

## context-engineering · 10

- **`agent-context-compaction`** — Keep long-running / multi-turn agents cheap AND sharp by compacting context before the window fills
- **`agent-context-db`** — Give a project agent ONE self-evolving "context database" that unifies episodic memory (what happened), semantic RAG (docs/knowledge) and sk
- **`agent-memory-file`** — Use the filesystem as durable, out-of-context memory for a Claude agent — scoped CLAUDE.md rule files, a NOTES.md progress ledger that survi
- **`claude-context-editing`** — Configure Anthropic's SERVER-SIDE context editing so heavy tool-use agents self-prune cache-safely instead of you hand-rolling compaction
- **`context-quality-evals`** — Evaluate the CONTEXT itself, not just the model — run needle-in-a-haystack / RULER-style sweeps, lost-in-the-middle position tests, distract
- **`context-window-budgeter`** — Allocate a fixed token budget across competing context sources (system rules, tools, retrieved docs, memory, chat history) by priority, then
- **`managed-agent-memory`** — Wire a managed memory service into an agent instead of hand-rolling memory files — Mem0 (extract/store/search facts), Zep + Graphiti (tempor
- **`prompt-compression`** — Shrink bloated prompts, few-shot exemplars and RAG context 2-20x with LLMLingua-2 token pruning BEFORE they hit the LLM, cutting input-token
- **`structured-memory-layers`** — Give an agent tiered durable memory that survives across sessions — pick the right tool for the need and wire it in
- **`subagent-context-isolation`** — Keep the orchestrator's context lean by delegating deep work to sub-agents with scoped, isolated context windows and artifact-backed hand-of

## data-analysis · 43

- **`bulk-content-extraction`** — Extract clean text plus structured metadata from thousands of heterogeneous files (PDF, DOCX, PPTX, XLSX, HTML, EML, EPUB, RTF, ODT, images)
- **`clickhouse-realtime-analytics`** — Build real-time columnar OLAP over event streams with ClickHouse — ingest millions of events/sec, pre-aggregate on write via incremental mat
- **`corpus-dedup-pipeline`** — Deduplicate large text corpora with a tiered exact -> fuzzy (MinHash/LSH) -> semantic (embeddings) pipeline
- **`dagster-asset-pipelines`** — Build software-defined asset (SDA) pipelines with Dagster — a typed, lineage-aware dependency graph where each node is a data asset (a table
- **`data-contracts`** — Author and enforce data contracts as version-controlled YAML using the Open Data Contract Standard (ODCS v3) and the datacontract-cli
- **`data-processing`** — Clean, transform, deduplicate, normalize, and reshape tabular data with verification at every step
- **`data-quality-validation`** — Validate a dataframe or CSV/Parquet with declarative schema + rule checks before you trust it — use Pandera (typed DataFrameModel, lazy erro
- **`data-schema`** — Inspect, validate, document, and design tabular data schemas with verification at every step
- **`dbt-analytics-engineering`** — Build a production dbt Core project the way analytics engineers actually do it — sources + freshness, staging/intermediate/marts layering, g
- **`dlt-python-pipelines`** — Build declarative extract-and-load pipelines in pure Python with dlt (dlthub) — pull from REST APIs, SQL databases, or files and land them i
- **`duckdb-analytics`** — Run fast in-process analytical SQL over Parquet/CSV/JSON/Arrow — directly on local files, globs, S3, or HTTPS — with zero server setup and l
- **`ducklake-lakehouse`** — Stand up a real lakehouse with DuckLake — a SQL catalog (SQLite/Postgres/DuckDB) for metadata plus Parquet data files on local disk or S3 —
- **`embedding-corpus-clustering`** — Turn a pile of unlabeled text (docs, tickets, reviews, headlines, transcripts, survey verbatims) into interpretable topics/clusters using se
- **`excel-forecasting-formulas`** — Build formula-only time-series forecasts inside an Excel workbook — no add-ins, no Python
- **`excel-monte-carlo-formulas`** — Run a Monte-Carlo simulation in Excel using formulas ONLY — no VBA, no @RISK, no add-ins
- **`exploratory-data-analysis`** — Produce rigorous exploratory analysis of a dataset — distributions, central tendency, dispersion, correlations, outliers — with every report
- **`firecrawl-scrape`** — Turn any URL or entire website into clean, LLM-ready markdown or schema-validated JSON using Firecrawl — scrape (one page), crawl (a whole s
- **`free-api-catalogue`** — Curated, verified lookup index of high-value FREE / public APIs — FX & crypto, weather & climate, geocoding & places, government & economic
- **`geocoding-places-api`** — Turn addresses and place names into lat/lon and back, then enrich with POIs, administrative boundaries, road distances and drive/walk-time i
- **`geospatial-analysis`** — Turn location data into analysis and maps with GeoPandas + leafmap — load shapefiles/GeoJSON, build points from lat/lon, reproject CRS, run
- **`govt-open-data-api`** — Pull real government & open-data programmatically from CKAN and Socrata portals (data.gov, data.gov.uk, data.cityofchicago.org, thousands mo
- **`incremental-content-index`** — Maintain a resumable, content-addressable index over a growing file collection so re-runs process ONLY new or changed files, never the whole
- **`interactive-web-maps`** — Render location data as interactive web maps with MapLibre GL + deck.gl — choropleths shaded by any metric, store/dealer locators with popup
- **`magika-file-triage`** — Detect the TRUE content type of huge, mislabeled, or extensionless file collections and route each file to the right parser using Google Mag
- **`marimo-reactive-notebook`** — Build reactive Python notebooks with marimo — notebooks stored as plain .py (git-diffable, importable, no hidden state), where changing one
- **`market-data-api`** — Pull finance and macroeconomic data from free public APIs into tidy, date-aligned datasets
- **`mathematical-computation`** — Solve mathematics problems — algebra, calculus, linear algebra, ODEs, probability, combinatorics — with symbolic computation in SymPy AND nu
- **`news-sentiment-api`** — Pull free news coverage volume, tone and entity signals from GDELT DOC 2.0 (global tone/volume firehose, no key), Marketaux (financial news
- **`parquet-arrow-optimization`** — Design columnar storage that actually reads fast — pick compression (zstd vs snappy) and tune row-group size, partition + sort files for pre
- **`perceptual-image-dedup`** — Find exact and near-duplicate images across huge photo/asset libraries
- **`polars-dataframes`** — Reach for Polars when pandas is too slow or blows past RAM
- **`resilient-scraper`** — Build a polite, reliable HTTP scraper in Python that doesn't get banned or drop data — httpx (async + HTTP/2) with tenacity exponential back
- **`sitemap-crawl-harvest`** — Enumerate a site's FULL URL surface before you crawl
- **`sqlmesh-transformations`** — Build in-warehouse SQL transformation pipelines with SQLMesh — the dbt alternative that gives you virtual data environments (zero-copy dev v
- **`statistical-testing`** — Run the correct statistical test for the user's question, check assumptions before running it, and report effect size + confidence interval
- **`stealth-browser-scraping`** — Get past Cloudflare Turnstile, DataDome, Akamai, PerimeterX and Kasada bot walls when plain requests/httpx and vanilla headless Chrome/Playw
- **`structured-page-extraction`** — Turn one raw HTML page into typed, schema-validated records
- **`synthetic-data-generation`** — Generate realistic synthetic datasets that preserve the statistical structure of a real table — reach for SDV (GaussianCopula / CTGAN / TVAE
- **`time-series-forecasting`** — Forecast a time series in Python with Nixtla StatsForecast (AutoARIMA, AutoETS, AutoTheta, MSTL) or TimeGPT — backtest with rolling cross-va
- **`weather-climate-api`** — Pull weather forecasts, ERA5 historical climate reanalysis (1940→now), and air-quality from free APIs to use as model covariates or narrativ
- **`web-change-monitor`** — Watch web pages for MEANINGFUL changes, not raw byte churn
- **`wikidata-knowledge-api`** — Query structured world knowledge and public-attention signals from Wikimedia — Wikidata SPARQL for the entity graph (occupations, birthplace
- **`zero-shot-auto-tagging`** — Auto-tag and classify a large document collection into a KNOWN taxonomy without labelled training data

## devops · 9

- **`claude-code-headless-ci`** — Run Claude Code unattended in pipelines — `claude -p` (print/headless mode) scoped with `--allowedTools`, locked down with `--permission-mod
- **`dockerfile-and-compose-authoring`** — Author lean, secure Dockerfiles and Compose files and lint them with hadolint
- **`github-actions-pipelines`** — Build robust, secure GitHub Actions CI/CD — matrix builds, dependency caching, reusable + composite workflows, OIDC cloud auth (no long-live
- **`gitops-argocd-flux`** — Set up pull-based GitOps delivery with Argo CD or Flux — git as the single source of truth, a controller that continuously reconciles the cl
- **`incident-response-and-postmortem`** — Run a live production incident and its blameless postmortem end to end
- **`kubernetes-workload-deploy`** — Ship a containerised app to Kubernetes properly
- **`load-testing-k6`** — Write and run k6 (or Locust) load tests that model real traffic — ramping VUs, constant arrival-rate spikes, staged soak tests — and gate CI
- **`opentelemetry-observability-slo`** — Verb-first trigger when you must instrument a service with OpenTelemetry (traces, metrics, logs), stand up an OTel Collector, ship signals t
- **`terraform-iac-modules`** — Author production Terraform / OpenTofu — reusable modules with typed variables + outputs, remote state with locking (S3 native lockfile or D

## documents · 25

- **`accessible-document-remediation`** — Use to make a PDF accessible and legally compliant — tag it for screen readers (PDF/UA-1 / ISO 14289), fix WCAG 2.1 AA structure, add alt te
- **`board-pack-exec-brief`** — Write dense, decision-first executive briefs — board pre-reads, ExCo/SLT decision memos, Amazon-style 6-pagers, options papers, and one-page
- **`claude-excel-addin-ops`** — Drive the Claude for Excel add-in (the in-app sidebar, NOT file-level .xlsx editing) to change model assumptions while preserving formula de
- **`contract-review`** — Review commercial contracts — MSAs, SOWs, NDAs, DPAs, order forms — the way a deal lawyer does: extract every obligation, deadline, payment
- **`data-driven-deck-generator`** — Generate a data-driven PowerPoint deck where every chart and headline is bound to real data — read a CSV/DataFrame/dict, build NATIVE editab
- **`dcf-lbo-valuation-model`** — Build a fully-linked DCF and LBO valuation in one Excel workbook — formulas only, zero hardcoded outputs — so every cell recalculates live w
- **`doc-coauthoring`** — Guide users through a structured workflow for co-authoring documentation
- **`document-assembly-mail-merge`** — Mass-produce personalised Word documents from ONE .docx template plus a data source (CSV / JSON / Excel) using docxtpl (python-docx-template
- **`docx`** — Use this skill whenever the user wants to create, read, edit, or manipulate Word documents (.docx files)
- **`docx-redline-compare`** — Generate a Word tracked-changes redline (native w:ins / w:del markup) between two .docx versions so reviewers see exactly what changed and c
- **`editorial-style-linter`** — Enforce a deterministic editorial house style with Vale — flag banned terms, UK/US spelling drift, wordy phrases, inconsistent capitalisatio
- **`excel-dynamic-array-formulas`** — Replace VLOOKUP-drag, helper columns, and manual pivot tables with spilling dynamic-array formulas — FILTER, SORT, SORTBY, UNIQUE, SEQUENCE
- **`excel-kpi-dashboard-formulas`** — Build a single-screen, formula-driven KPI dashboard in Excel with zero VBA or macros — a SWITCH/CHOOSE metric selector wired to a dropdown
- **`excel-lambda-functions`** — Build reusable, named custom functions in Excel with LET and LAMBDA — no VBA, no add-ins
- **`excel-model-audit`** — Error-proof and stress-test a spreadsheet financial model against FAST and ICAEW standards
- **`excel-scenario-sensitivity`** — Build deterministic what-if analysis in a real .xlsx — native one- and two-variable Data Tables (Excel's {=TABLE()} array via openpyxl DataT
- **`google-workspace-authoring`** — Author and edit native Google Slides, Docs, and Sheets programmatically via the batchUpdate APIs — build decks, briefs, and trackers with re
- **`i18n-localization-qa`** — QA a localisation before it ships
- **`internal-comms`** — A set of resources to help me write all kinds of internal communications, using the formats that my company likes to use
- **`meeting-intelligence`** — Turn a meeting recording or raw transcript into a clean deliverable pack: minutes, a decisions log, an owner/deadline action table, and read
- **`pdf`** — Use this skill whenever the user wants to do anything with PDF files
- **`pptx`** — Use this skill any time a .pptx file is involved in any way — as input, output, or both
- **`rfp-proposal-response`** — Run a solicitation (RFP/RFI/ITT/PQQ/tender) from receipt to submission-ready draft the way a new-business/bid team does: shred the document
- **`three-statement-financial-model`** — Build a fully-linked three-statement financial model (income statement, balance sheet, cash flow) in Excel with FORMULAS ONLY — every foreca
- **`xlsx`** — Use this skill any time a spreadsheet file is the primary input or output

## engineering-workflow · 39

- **`api-contract-design`** — Design and lint a REST API contract properly — author OpenAPI 3.1 (JSON Schema 2020-12) with reusable components, enforce house style with a
- **`ast-grep-codemod`** — Run structural (AST-based) search-and-rewrite across a codebase with ast-grep — tree-sitter patterns, `$META` capture variables, YAML rule f
- **`autonomy-policy`** — Use at the start of EVERY task to decide whether to proceed autonomously or pause and converse with the user
- **`brainstorming`** — You MUST use this before any creative work - creating features, building components, adding functionality, or modifying behavior
- **`changelog-release-automation`** — Automate versioning and releases from git history — wire up Conventional Commits, generated CHANGELOGs, SemVer bumps, git tags, and GitHub R
- **`claude-code-hooks-authoring`** — Author Claude Code settings.json hooks against the full 2026 event catalogue
- **`coding-standards`** — Baseline cross-project coding conventions for naming, readability, immutability, and code-quality review
- **`continuous-learning-v2`** — Instinct-based learning system that observes sessions via hooks, creates atomic instincts with confidence scoring, and evolves them into ski
- **`debugging-and-error-recovery`** — Guides systematic root-cause debugging
- **`dependency-upgrade-migration`** — Upgrade dependencies without breaking the build
- **`dispatching-parallel-agents`** — Use when facing 2+ independent tasks that can be worked on without shared state or sequential dependencies
- **`email-deliverability`** — Diagnose and fix why email lands in spam or gets rejected
- **`executing-plans`** — Use when you have a written implementation plan to execute in a separate session with review checkpoints
- **`finishing-a-development-branch`** — Use when implementation is complete, all tests pass, and you need to decide how to integrate the work - guides completion of development wor
- **`flaky-test-detection`** — Catch, quarantine, and root-cause flaky tests — the ones that pass and fail on IDENTICAL code and poison CI trust
- **`flow-reader`** — Read, interpret, and act on any flow-shaped diagram — UI/UX site maps, user journeys, state machines, agent graphs, workflow diagrams, syste
- **`git-workflow`** — Git workflow patterns including branching strategies, commit conventions, merge vs rebase, conflict resolution, and collaborative developmen
- **`incremental-implementation`** — Delivers changes incrementally
- **`karpathy-guidelines`** — Behavioral guidelines to reduce common LLM coding mistakes
- **`lsp-code-navigation`** — Wire a real Language Server (tsserver, pyright, gopls, rust-analyzer) into an agent over the jonrad/lsp-mcp MCP bridge so it answers go-to-d
- **`mutation-testing`** — Measure the REAL quality of a test suite by injecting mutants (flip `>` to `>=`, `+` to `-`, delete a return) and checking whether tests cat
- **`openapi-client-codegen`** — Generate a fully typed API client from an OpenAPI/Swagger spec with orval — TypeScript models, fetch/axios call functions, Zod request+respo
- **`property-based-testing`** — Stop hand-picking example inputs — assert PROPERTIES ("output is always sorted", "decode(encode(x))==x") and let a generator hurl hundreds o
- **`receiving-code-review`** — Use when receiving code review feedback, before implementing suggestions, especially if feedback seems unclear or technically questionable -
- **`repo-context-packer`** — Pack a whole codebase, subfolder, or remote GitHub repo into ONE AI-ready context bundle with repomix — directory tree + per-file contents
- **`requesting-code-review`** — Use when completing tasks, implementing major features, or before merging to verify work meets requirements
- **`security-review`** — Use this skill when adding authentication, handling user input, working with secrets, creating API endpoints, or implementing payment/sensit
- **`semantic-code-search`** — Stand up a local-first semantic (embedding) code index with SeaGOAT so an agent finds code by MEANING, not literal grep — ask "where do we r
- **`source-driven-development`** — Grounds every implementation decision in official documentation
- **`spec-driven-development`** — Creates specs before coding
- **`subagent-driven-development`** — Use when executing implementation plans with independent tasks in the current session
- **`systematic-debugging`** — Use when encountering any bug, test failure, or unexpected behavior, before proposing fixes
- **`tdd-workflow`** — Use this skill when writing new features, fixing bugs, or refactoring code
- **`test-driven-development`** — Use when implementing any feature or bugfix, before writing implementation code
- **`using-git-worktrees`** — Use when starting feature work that needs isolation from current workspace or before executing implementation plans - creates isolated git w
- **`using-superpowers`** — Use when starting any conversation - establishes how to find and use skills, requiring Skill tool invocation before ANY response including c
- **`verification-before-completion`** — Use when about to claim work is complete, fixed, or passing, before committing or creating PRs - requires running verification commands and
- **`verification-loop`** — A comprehensive verification system for Claude Code sessions
- **`writing-plans`** — Use when you have a spec or requirements for a multi-step task, before touching code

## finance-ops · 1

- **`finance-accounting-ops`** — Run daily bookkeeping against Xero and QuickBooks Online: parse invoices and receipts into structured lines, reconcile bank statements to th

## frontend-and-design · 57

- **`accessibility`** — Design, implement, and audit inclusive digital products using WCAG 2.2 Level AA standards
- **`accessible-contrast-checker`** — Check AND auto-fix colour contrast before you ship a palette
- **`ag-ui-agent-frontend`** — Wire an AG-UI-speaking agent backend into a React/Next.js frontend with CopilotKit — stream agent events (text, tool calls, shared state, re
- **`agent-browser`** — Drive a real Chrome browser from the CLI to view, test, screenshot and debug web pages — the visual-QA loop for any frontend work
- **`agentic-web-automation`** — Automate multi-step, natural-language web tasks when CSS/XPath selectors are too brittle to maintain — drive an LLM agent loop that observes
- **`ai-powered-artifact-app`** — Build a shareable Claude Artifact that calls Claude from INSIDE the running app via window.claude.complete, so recipients use your AI tool w
- **`algorithmic-art`** — Creating algorithmic art using p5.js with seeded randomness and interactive parameter exploration
- **`brand-color-token-system`** — Turn ONE brand hex into a complete 50->950 tonal ramp with a semantic token layer and light/dark parity, exported as CSS custom properties
- **`brand-guidelines`** — Applies Anthropic's official brand colors and typography to any sort of artifact that may benefit from having Anthropic's look-and-feel
- **`brand-voice`** — Build a source-derived writing style profile from real posts, essays, launch notes, docs, or site copy, then reuse that profile across conte
- **`browser-qa`** — Use this skill to automate visual testing and UI interaction verification using browser automation after deploying features
- **`browser-testing-with-devtools`** — Tests in real browsers
- **`canvas-design`** — Create beautiful visual art in .png and .pdf documents using design philosophy
- **`claude-chrome-workflow-recorder`** — Author durable, repeatable Claude-in-Chrome routines for a live authenticated browser session
- **`click-path-audit`** — Trace every user-facing button/touchpoint through its full state change sequence to find bugs where functions individually work but cancel e
- **`code-review-and-quality`** — Conducts multi-axis code review
- **`color-harmony-generator`** — Generate complementary, analogous, triadic, split-complementary, tetradic and square colour harmonies by rotating hue in OKLCH so every swat
- **`color-psychology-advertising`** — Choose ad and brand colours using what the peer-reviewed evidence actually supports — and refuse to over-claim
- **`colorblind-safe-palettes`** — Pick, validate, and repair categorical colour palettes for colour-vision deficiency (protan/deutan/tritan ~8% of men, 0.5% of women)
- **`dashboard-information-architecture`** — Lay out data-dense dashboards so they actually get READ — decide KPI hierarchy, scan order, density-tier grids, and drill-down layering BEFO
- **`design-approval-gate`** — Use BEFORE shipping any visual or UI change — new components, new pages, landing pages, hero sections, theme changes, ui-effects integration
- **`design-system`** — Use this skill to generate or audit design systems, check visual consistency, and review PRs that touch styling
- **`design-tokens-pipeline`** — Build a full W3C DTCG design-token pipeline with Style Dictionary v5 — spacing, radius, shadow, typography, motion/duration, and color — com
- **`filmic-static-panels`** — Recreate the cinematic "static" container look — dark gradient panels (raised→surface, hairline border, soft radius, depth shadow) sitting u
- **`fluid-responsive-system`** — Generate breakpoint-free fluid type, space and grid using CSS clamp() interpolation — the Utopia method — so headings, body text, gaps and m
- **`frontend-design`** — Create distinctive, production-grade frontend interfaces with high design quality
- **`frontend-ui-engineering`** — Builds production-quality UIs
- **`generative-ui-chat-interface`** — Build a streaming AI chat / generative-UI front-end with the Vercel AI SDK v7 (useChat + DefaultChatTransport) and AI Elements
- **`html-email-builder`** — Build client-proof HTML email that survives Outlook, Gmail and Apple Mail — pick MJML, React Email or Maizzle, get table-based layout with i
- **`icon-system`** — Stand up ONE consistent icon system instead of pasting random SVGs from everywhere — pick a source (Lucide for a curated stroke set, Iconify
- **`image-palette-extraction`** — Extract a dominant palette from a photo, poster, or logo and turn it into a usable theme — quantise pixels (median-cut, k-means/WSMeans, or
- **`live-artifact-dashboard-builder`** — Build Claude Cowork Live Artifacts — persistent, self-refreshing HTML dashboards that Claude re-queries from your connected apps (Calendar
- **`motion-system`** — Define a reusable motion design system instead of hand-tuning every transition — a small named scale of duration, easing, spring and stagger
- **`oklch-color-engine`** — Do perceptually-uniform colour work in OKLCH/OKLab — parse any input, convert to OKLCH, build even lightness/chroma ramps, interpolate gradi
- **`perceptual-gradient-designer`** — Design banding-free multi-stop gradients by interpolating in OKLab/OKLCH instead of naive sRGB — kills the grey dead-zone where blue→yellow
- **`performance-optimization`** — Optimizes application performance
- **`print-editorial-layout`** — Turn HTML/CSS into print-quality paginated PDFs with Paged.js
- **`professional-page-templates`** — Build professionally-designed marketing pages, product sites, portfolios, dashboards, conference pages, newsrooms, and experiential scrollyt
- **`pwa-installable-offline`** — Make a Vite web app installable and offline-capable with vite-plugin-pwa (Workbox under the hood)
- **`quick-chart`** — Render ONE correct chart from a dataset in a single self-contained HTML file — auto-pick the right chart type (bar/line/scatter/doughnut) fr
- **`quick-dashboard`** — Turn CSV, JSON, or pasted rows into ONE self-contained responsive dashboard HTML file — a KPI stat row, 2–3 Chart.js charts, and one sortabl
- **`quick-form`** — Ship a production-quality contact / signup / feedback form as ONE self-contained HTML file — semantic fields, real-time inline validation, a
- **`quick-landing`** — Ship one polished, single-file HTML + Tailwind (Play CDN) landing page in under 2 minutes, no build step — semantic hero, exactly 3 feature
- **`quick-microsite`** — Ship a polished 2-4 "page" static microsite (event, product launch, personal profile, docs-lite) as ONE self-contained HTML file using ancho
- **`quick-tool`** — Ship one single-purpose interactive tool — calculator, unit/currency converter, generator (password, slug, QR-input, color), picker, scorer/
- **`recording-studio`** — Stand up a branded, deploy-ready artist cultural-intelligence & campaign-tracking platform ("The Recording Studio") for a Warner Music artis
- **`shadcn-tailwind-v4-stack`** — Scaffold the dominant 2026 React build stack — Tailwind CSS v4 (CSS-first, zero config file) plus shadcn/ui components — the correct, non-br
- **`svg-illustration-animation`** — Animate SVG illustrations with GSAP's now-free (since 2025) plugins — DrawSVGPlugin for self-drawing line art, MorphSVGPlugin for shape-to-s
- **`theme-factory`** — Toolkit for styling artifacts with a theme
- **`typography-type-system`** — Build a real typography system, not just font-sizes — a modular scale with a named ratio, fluid clamp() steps, variable-font axes wired corr
- **`ui-demo`** — Record polished UI demo videos using Playwright
- **`vccp-logo-use`** — Apply the four official VCCP bear-and-girl logo lockups — `Logo.png`, `Bear_Lockup.png`, `Girl_Lockup.png`, `Girl_and_Bear.png` — to client-
- **`vccp-media-design`** — Apply the VCCP Media 2026 brand system to any client-facing artifact — web pages, web apps, dashboards, slide decks (PPTX / Keynote / Google
- **`visual-regression-testing`** — Set up pixel-diff screenshot baseline testing so UI changes are caught by comparing rendered screenshots against approved baselines
- **`web-artifacts-builder`** — Suite of tools for creating elaborate, multi-component claude.ai HTML artifacts using modern frontend web technologies (React, Tailwind CSS
- **`webapp-testing`** — Toolkit for interacting with and testing local web applications using Playwright
- **`webgl-3d-scene`** — Build a production-grade 3D scene on the web with React Three Fiber (Three.js) — a Canvas with proper camera and lighting rigs, GLTF model l

## marketing-science · 4

- **`geo-incrementality-testing`** — Design and analyse geo holdout / geo-lift experiments to measure the TRUE incremental effect of a media campaign — not correlated last-click
- **`marketing-mix-modeling`** — Build a Bayesian marketing-mix model to measure media ROI, decompose sales, and optimise budget
- **`multi-touch-attribution`** — Attribute conversions across the multi-touch customer journey with data-driven models, not just last-click
- **`uplift-modeling`** — Model who a campaign actually CHANGES — not who converts anyway — with uplift / heterogeneous-treatment-effect models, then target the persu

## mcp-connectors · 9

- **`connect-database-mcp`** — Wire a SQL database into Claude Code over MCP for safe, read-only querying — Postgres, MySQL, SQLite, BigQuery (and 20+ more) via the google
- **`connect-github-mcp`** — Wire up GitHub's OFFICIAL MCP server so Claude can drive repos, issues, PRs, code search, and Actions natively — no `gh` binary, no shelling
- **`connect-playwright-mcp`** — Register and drive a real browser over MCP with Microsoft's Playwright MCP server (@playwright/mcp)
- **`connect-public-api`** — Talk to ANY public/REST API robustly when you have a URL and docs but no ready-made SDK or MCP server
- **`connect-web-fetch-scrape-mcp`** — Wire up an MCP server to pull web content into Claude — the lightweight Fetch server (URL -> markdown, no key) vs Firecrawl (JS-rendered pag
- **`mcp-apps-interactive-ui`** — Build MCP Apps (the ext-apps / SEP-1865 extension, spec 2026-01-26) so an MCP tool returns an INTERACTIVE UI — a chart, form, picker, dashbo
- **`mcp-server-security`** — Secure an MCP server and vet third-party ones before you connect
- **`mcp-tool-gateway-router`** — Fight MCP tool sprawl and context bloat when a session aggregates many servers (GitHub + Slack + Sentry + DB + Adobe = 200+ tools, ~55k toke
- **`register-mcp-servers`** — Install, register, and manage MCP servers in Claude Code, Claude Desktop, and remote clients

## media · 40

- **`ai-upscale-restore`** — AI-upscale and restore low-res or degraded images and video, and fix damaged faces
- **`audio-loudness-ducking`** — Fix audio levels the right way with ffmpeg: two-pass EBU R128 loudnorm to hit an exact platform target (-14 LUFS for YouTube/Spotify/TikTok/
- **`audio-mastering-chain`** — Master and finish a mix to a delivery-ready file with Matchering + ffmpeg
- **`auto-silence-cut`** — Auto jump-cut talking-head, tutorial, podcast, or screen-recording footage by detecting and removing (or speeding up) the silent gaps — dead
- **`background-removal-batch`** — Batch-remove image backgrounds to transparent PNGs (real alpha) using rembg with BiRefNet / BRIA RMBG / IS-Net / U²-Net models, plus optiona
- **`batch-transcode-encode`** — Batch-transcode a whole FOLDER of videos to clean H.264/H.265 MP4, or make lightweight editing PROXIES, with the RIGHT codec + quality choic
- **`beat-synced-edit`** — Cut video to the beat of music — detect the beat grid, downbeats, or transient onsets in a track with librosa (beat_track / onset_detect), t
- **`channel-playlist-archive`** — Bulk-lift a whole YouTube channel or playlist with yt-dlp, using a --download-archive file so re-runs SKIP everything already grabbed (perfe
- **`comfyui-workflow-runner`** — Headlessly execute a ComfyUI workflow from the command line via its HTTP + WebSocket API — POST the API-format node graph to /prompt, track
- **`contact-sheet-storyboard`** — Turn a video into a single contact-sheet / storyboard grid — evenly-spaced thumbnails tiled into rows and columns with the source timecode b
- **`controllable-video-to-video`** — Restyle, relight, recompose, or VFX an EXISTING clip while keeping its motion — video-to-video, not fresh generation
- **`ffmpeg-cookbook`** — Battle-tested, copy-pasteable ffmpeg recipes with the CORRECT flags for each job: losslessly concat/stitch clips, trim/cut without re-encodi
- **`flux-image-gen`** — Generate or instruction-edit images with the Flux family (FLUX.2 / FLUX.1 Kontext / Pro / dev) via the Black Forest Labs API, fal.ai, or Rep
- **`frame-interpolation-retiming`** — Generate synthetic in-between frames to make video smoother — buttery slow-motion from ordinary 30fps footage, or up-convert a clip's frame
- **`hdr-tonemap-color`** — Fix washed-out, grey, milky, or over-bright HDR video by correctly tonemapping HDR (BT.2020 PQ/HLG) down to SDR (BT.709) with ffmpeg zscale+
- **`keyframe-motion-frame-diff`** — Frame-diff analysis for video: collapse thousands of near-identical frames into a handful of UNIQUE keyframes using perceptual hashing (phas
- **`lip-sync-avatar`** — Make a face TALK — drive lips on a photo or video clip from any audio/VO so a spokesperson, presenter, or dubbed actor mouths the words
- **`long-video-to-shorts`** — Turn ONE long video (podcast, interview, webinar, lecture, stream VOD) into a ranked set of vertical 9:16 shorts — transcribe it, find and s
- **`lottie-motion-graphics`** — Generate and template Lottie JSON animations in code, then render them to MP4/GIF/WebM — for versioned logo stings, animated lower-thirds, a
- **`media-toolchain-bootstrap`** — One-shot macOS setup or repair of the local media toolchain WITHOUT Homebrew or sudo: installs the standalone yt-dlp_macos binary, a portabl
- **`music-generation-jingle`** — Generate original music, jingles, stings, loops, and underscores from a text prompt using open models (MusicGen / AudioCraft, Stable Audio O
- **`nano-banana-image`** — Generate and edit images with Google's Nano Banana (Gemini Flash Image, gemini-2.5-flash-image) via the Gemini API — the model that actually
- **`open-video-gen-wan`** — Self-host open-weight text-to-video and image-to-video on your OWN GPU with license-clean models — Wan 2.2 (MoE A14B + the 24GB-friendly TI2
- **`sfx-foley-generation`** — Generate sound effects and foley from a TEXT prompt — footsteps, whooshes, impacts, UI clicks, ambience, stingers — via ElevenLabs SFX v2 (c
- **`shot-scene-detection`** — Detect every shot boundary / scene cut in a video and produce a timecoded shot list plus split clips
- **`slack-gif-creator`** — Knowledge and utilities for creating animated GIFs optimized for Slack
- **`social-video-reframe`** — Reframe (recrop) horizontal 16:9 video into vertical 9:16, square 1:1, or portrait 4:5 for Reels, Shorts, and TikTok — with subject-aware sm
- **`stem-separation`** — Split a song into separate stems — vocals, drums, bass, and other (or guitar/piano with the 6-source model) — using Demucs (Hybrid Transform
- **`typographic-image-gen`** — Generate ad comps where the IN-IMAGE TEXT is legible, correctly spelled, and on-brand — OOH/billboard posters, pack shots, price flashes, me
- **`video-audio-rip`** — Rip audio-only files (mp3 / m4a / opus / flac / wav) from a YouTube URL, podcast page, or a local video file — with embedded cover art and c
- **`video-clip-extractor`** — Grab ONLY the part you want out of a video — a single timestamp range (e.g
- **`video-frame-extraction`** — Extract video frames to PNG/JPG with ffmpeg — pull every frame, every Nth frame, one per second, keyframes only, or a single frame at an exa
- **`video-gen-pipeline`** — Generate short AI video clips from text (or a first-frame image) with the current model catalogue — Veo 3.1, Kling 2.1/3.0 Pro, Seedance 2.0
- **`video-matting-rotoscope`** — Pull a temporally-stable alpha matte off a person/subject across a whole video with RobustVideoMatting (RVM) — clean, flicker-free green-scr
- **`video-object-tracking-sam2`** — Track, segment and mask ONE (or several) objects across every frame of a video with SAM 2 / SAM 2.1 from a single click, box or point prompt
- **`video-ocr-onscreen-text`** — Read text that is baked into a video's pixels — rip hardcoded/burned-in subtitles to a timed SRT, and log every lower-third, name super, chy
- **`voice-clone-tts`** — Generate spoken narration and voiceover LOCALLY — few-shot voice cloning from a ~10s reference clip (Chatterbox / GPT-SoVITS) or fast preset
- **`whisper-caption-burn`** — Generate word-level Whisper captions and turn them into animated TikTok/Reels-style karaoke subtitles (active word pops + accent colour), th
- **`youtube-download`** — Download a YouTube (or any yt-dlp-supported) video to a local file with robust format/resolution choice, and survive 2026-era breakage: SABR
- **`youtube-transcript-lift`** — Pull a clean, readable text transcript out of a YouTube (or any yt-dlp-supported) video WITHOUT downloading the video

## meta · 1

- **`sebduffy`** — The one-upload door to the entire SEBSKILLS library

## meta-router · 8

- **`automatic-skill-decision`** — Given any user request, silently pick the right skill(s) from a SKILL.md manifest by scoring trigger keywords + semantic intent, then apply
- **`claude-code-plugin-builder`** — Scaffold, wire and ship a multi-component Claude Code plugin — one versioned bundle of skills, subagents, slash commands, hooks, output styl
- **`requirement-elicitation`** — Detect when a request is underspecified and, before acting, ask the minimum set of high-value clarifying questions that fully dissect the ta
- **`skill-adder`** — Contribute a NEW skill into the SEBSKILLS library through a guarded, gated intake — a "PR-merge for skills"
- **`skill-auditor`** — Audit a SEBSKILLS skill for both correctness AND whether it actually works — a STATIC pass (all 10 frontmatter fields present, name==folder
- **`skill-chaining-composer`** — Turn ONE compound request into an ordered chain of skills: decompose the job into sub-tasks, map each sub-task to the right skill, order the
- **`skill-gap-detector`** — Detect when NO existing skill covers the user's request, name the missing capability precisely, and hand off to skill-creator to author it
- **`skill-marketplace-packager`** — Package SEBSKILLS skills as portable, harness-agnostic bundles so the library installs beyond Claude Code — into Codex CLI, Cursor, OpenCode

## ml · 8

- **`build-train-gnn`** — Build and train Graph Neural Networks with PyTorch Geometric (PyG)
- **`conformal-prediction`** — Wrap ANY trained model (sklearn, XGBoost, TabPFN, even an API model) with conformal prediction to get distribution-free, calibrated uncertai
- **`embedding-model-training`** — Fine-tune embedding (bi-encoder) and reranker (cross-encoder) models with Sentence Transformers v3/v4 — pick the right loss for your data sh
- **`gradient-boosting-tabular`** — Build a strong tabular ML model with gradient-boosted trees (XGBoost / LightGBM / CatBoost) for churn, propensity, lead-scoring, conversion
- **`lora-qlora-finetune`** — Fine-tune an open LLM cheaply with LoRA/QLoRA
- **`ml-model-eval`** — Evaluate a trained model like an adult instead of quoting a single accuracy number
- **`neural-net-from-scratch`** — Write your own PyTorch training loop from bare metal — Dataset/DataLoader, forward/backward, mixed-precision (AMP), gradient accumulation, L
- **`tabular-foundation-model`** — Predict on small tabular datasets with TabPFN — a pretrained transformer that does in-context tabular classification/regression in ONE forwa

## mobile · 1

- **`mobile-app-scaffold`** — Verb-first: scaffold a real React Native app with Expo, then build, submit, update, and push-notify it via EAS — the only path in this libra

## model-routing · 13

- **`batch-api-offloader`** — Cut LLM spend in half on non-realtime work by moving it to the Anthropic Message Batches API or OpenAI Batch API for a guaranteed 50% discou
- **`best-model-per-step-pipeline`** — Assign each STEP of a multi-step task to the model+vendor best at that step (deep reasoning on one, coding on another, vision on another, ch
- **`cache-aware-context-layout`** — Audit and restructure LLM prompts so the provider's prompt cache actually hits — turning 7% hit rates into 70%+
- **`cross-provider-gateway`** — Put ONE OpenAI-compatible endpoint in front of Anthropic, OpenAI, Google/Gemini, Mistral, Groq, and open-weight models so a single call site
- **`cross-vendor-llm-judge`** — Grade, verify, or debate one model's output using a judge from a DIFFERENT company — independent LLM-as-a-judge and voting panels that dodge
- **`llm-cost-estimator`** — Price an LLM call in USD BEFORE you run it
- **`mixture-of-models-ensemble`** — Fan one prompt to several models from different vendors in parallel, then have an aggregator model synthesize their drafts into one better a
- **`model-cascade-escalation`** — Run a cheap/small model first and escalate to a stronger (often different-vendor) model ONLY when a gate — self-reported confidence, an LLM
- **`model-routing-eval-benchmark`** — Benchmark models ACROSS vendors on YOUR OWN data to prove which model to route each task type to — then turn the results into a routing tabl
- **`model-triage-router`** — Route each request cheap-first inside ONE vendor's tier ladder (Anthropic Haiku -> Sonnet -> Opus, or OpenAI mini -> full) using a difficult
- **`output-token-diet`** — Cut OUTPUT tokens 40-70% — the side you're billed 4-5x more for — without losing information, by combining chain-of-draft reasoning, structu
- **`provider-failover-reliability`** — Make LLM calls survive rate-limits and provider outages by wiring automatic cross-vendor failover, retries with backoff, cooldowns, and cost
- **`semantic-response-cache`** — Put an embedding-similarity (semantic) cache in front of an LLM or RAG app so near-duplicate questions reuse a stored answer and skip infere

## product · 7

- **`deep-research`** — Multi-source deep research using firecrawl and exa MCPs
- **`documentation-and-adrs`** — Records decisions and documentation
- **`idea-refine`** — Refines ideas iteratively
- **`market-research`** — Conduct market research, competitive analysis, investor due diligence, and industry intelligence with source attribution and decision-orient
- **`product-capability`** — Translate PRD intent, roadmap asks, or product discussions into an implementation-ready capability plan that exposes constraints, invariants
- **`product-lens`** — Use this skill to validate the "why" before building, run product diagnostics, and pressure-test product direction before the request become
- **`shipping-and-launch`** — Prepares production launches

## rag · 10

- **`agentic-rag-pipeline`** — Build an agentic retrieval loop instead of one fixed top-k fetch — decompose the query, route sub-questions to the right source, grade every
- **`graphrag-builder`** — Build and query a knowledge-graph RAG system so you can answer multi-hop and global-summary questions that plain vector RAG cannot ("what ar
- **`hybrid-search-reranking`** — Build two-stage retrieval that fixes both recall and precision: fuse BM25/sparse lexical search with dense ANN vectors using Reciprocal Rank
- **`lancedb-multimodal-store`** — Build an embedded LanceDB/Lance multimodal store that keeps vectors, raw media bytes (images, audio, video frames, PDFs) and metadata togeth
- **`llm-rag-eval-harness`** — Build a regression-proof eval suite for a RAG or LLM app — a versioned golden dataset, the right retrieval + generation metrics (context pre
- **`long-doc-chunking`** — Split any long document, PDF, transcript, or source-code file into clean, structure-preserving chunks using Chonkie — pick token / recursive
- **`rag-chunking-contextual`** — Choose and implement the right RAG chunking + embedding strategy — fixed-token, recursive, semantic, hierarchical/parent-child, and late chu
- **`retrieval-as-context`** — Turn a pile of retrieved chunks into clean, ordered, citation-ready LLM context
- **`vector-store-setup`** — Pick, provision, and tune the vector store under a RAG app — pgvector, Qdrant, Chroma, or LanceDB
- **`visual-document-rag`** — Retrieve over PDF/deck/scan PAGES AS IMAGES using ColPali-family late-interaction multimodal models (ColQwen2.5, ColPali, ColSmol) — no OCR

## recipes · 12

- **`brand-kontext-studio`** — Recreate FLUX.1 Kontext / Nano-Banana style in-context brand image editing as a named combo
- **`computer-use-qa-runner`** — Recreate a trycua/cua- and Microsoft-Fara-style agentic browser-and-computer-use QA runner as a COMBO that chains existing library skills: a
- **`deepdish-research-desk`** — Recreate a DeerFlow / OpenAI Deep Research long-horizon research desk as a COMBO of proven SEBSKILLS
- **`insight-to-deck-autopilot`** — Recreate an agency's core money-maker — the research-to-branded-pitch-deck pipeline that tools like Gamma, Tome and Decktopus promise — as a
- **`kinetic-type-promo`** — Recreate After-Effects-style kinetic typography and motion-graphics promos entirely web-native (CSS + WebGL, no AE) as a named combo
- **`living-dashboard`** — Recreate a Hex / Observable-style self-updating analytics app as a COMBO of existing SEBSKILLS — pull live public data, transform it in-proc
- **`product-hero-3d`** — Recreate a Hunyuan3D-2 / TRELLIS style image-to-3D asset pipeline as a named combo: turn one clean product photo into a textured 3D mesh and
- **`reel-foundry`** — Recreate a Wan2.2 / FramePack-style automated ad-reel factory as a named combo — take a prompt list or a pile of supplied footage and turn i
- **`subagent-swarm`** — Recreate a portable agent-orchestration harness — the kind sold as a marketplace product (wshobson's agent marketplace, the openai-agents "h
- **`synthetic-audience-lab`** — Recreate a silicon-sampling / synthetic-persona message-testing lab (the "ask an AI audience before you spend" capability behind tools like
- **`temporal-memory-spine`** — Recreate a persistent, self-evolving agent-memory backbone — the Graphiti / OpenViking shape — as a COMBO of proven SEBSKILLS
- **`voiceover-dub-booth`** — Recreate an ElevenLabs / Chatterbox-style dubbing booth locally as a named combo — clone or preset-narrate a script into a voiceover, align

## sales-crm · 1

- **`crm-pipeline-automation`** — Read and write CRM records across HubSpot, Salesforce, and Pipedrive from the terminal — paginate contacts/deals, dedup by email/domain, enr

## security · 12

- **`artifact-signing-slsa-provenance`** — Prove build integrity, not just scan for CVEs
- **`claude-code-sandbox-config`** — Configure Claude Code's OS-level Bash sandbox for safe autonomy — scope filesystem write/read paths, set network domain allowlists, and bloc
- **`cloud-security-posture-cspm`** — Audit a LIVE cloud account (AWS, Azure, GCP, Kubernetes) for misconfigurations against CIS, NIST, PCI-DSS, SOC2 and friends using Prowler an
- **`container-iac-hardening`** — Scan container images, Dockerfiles, Kubernetes manifests and Terraform for CVEs and misconfigurations, then fix them
- **`dast-web-scan-zap-nuclei`** — Dynamically scan a RUNNING web app or API for live vulnerabilities using OWASP ZAP (baseline / full / API scan) and ProjectDiscovery Nuclei
- **`llm-red-team`** — Red-team an LLM app or model on YOUR OWN authorised targets
- **`pii-redaction-presidio`** — Detect, redact and anonymize PII/PHI in text at scale with Microsoft Presidio
- **`policy-as-code-opa-kyverno`** — Author, test, and enforce policy-as-code with OPA/Rego, Conftest, and Kyverno
- **`runtime-security-falco-ebpf`** — Deploy eBPF-based RUNTIME threat detection on Linux/Kubernetes hosts you are authorised to monitor
- **`sast-semgrep-opengrep`** — Run and author static-analysis (SAST) rules to catch injection, auth, path-traversal and secret bugs in YOUR OWN code with Semgrep or the Op
- **`secrets-hygiene-and-remediation`** — Hunt and kill leaked credentials
- **`supply-chain-sca-audit`** — Audit a repo's dependency supply chain end-to-end

## strategy · 32

- **`WLV`** — Write Like Vallance — produce writing in the voice of Charles Vallance (chairman and founding partner of VCCP): erudite, witty, metaphor-led
- **`advertising-strategy`** — Build an advertising / communications strategy from a client brief
- **`advertising-strategy-copy`** — Write the *prose* of an advertising / communications strategy: written strategy paragraphs, single-minded propositions, manifestos, strategi
- **`attention-planning-metrics`** — Plan and measure media with attention metrics — Adelaide AU, Lumen APM/aCPM, Amplified Intelligence active/passive seconds, TVision eyes-on-
- **`audience-insight`** — Excavate a usable human insight about an audience — the "unsaid truth" that a brand can credibly act on
- **`audience-segmentation`** — Build, name, profile, and *use* audience segmentations — behavioural, attitudinal, need-state, value-based, and occasion-based
- **`behavioural-science-comms`** — Apply behavioural science to advertising and comms — diagnose the target behaviour with COM-B, design the intervention with the BIT EAST fra
- **`brand-audit`** — Audit an existing brand's current comms, positioning and equity: distinctive brand assets, share of voice, share of search, share-of-categor
- **`brand-health-tracker-design`** — Design an ONGOING brand health tracker, not a one-off audit: pick the metric spine (funnel + equity pillars), set wave cadence and base size
- **`category-entry-points-mental-availability`** — Map a brand for growth the Ehrenberg-Bass way: elicit Category Entry Points (CEPs) with the 7 W's, score mental availability (mental market
- **`competitive-comms-audit`** — Audit competitor advertising and communications across paid, owned, earned and shared — to map who is saying what, in what codes, at what we
- **`creative-pretesting-framework`** — Design a real-world creative pretest (System1 Test Your Ad, Kantar LINK+, Neurons Predict AI) and interpret the scores into a hard go/refine
- **`cultural-semiotics`** — Decode cultural codes in a category — Residual (the codes fading), Dominant (the codes the category currently runs on), Emergent (the codes
- **`data-analyst`** — Proper data analyst workflow — exploratory data analysis (EDA), hypothesis testing, segmentation, cohort analysis, time-series decomposition
- **`data-cut-headline-stats`** — Cut and interrogate raw data (CSV, XLSX, brand tracker exports, media plan returns, campaign performance, sales data, social listening, sear
- **`deck-flow-structure`** — Plan the *flow* of a deck — the narrative architecture and the order of information — before any slide gets designed or filled with content
- **`developed-research`** — Build long-form developed research — immersive briefs, category POVs, sector reviews, audience deep dives, "state of X" reports, foresight p
- **`effectiveness-case`** — Write an effectiveness case — an IPA Effectiveness Awards-style write-up that argues, with evidence, that a piece of advertising made a meas
- **`geo-answer-engine-optimization`** — Optimise a brand, page or site to be RETRIEVED and CITED by AI answer engines (ChatGPT, Perplexity, Google AI Overviews, Claude, Gemini, Cop
- **`influencer-creator-strategy`** — Source, fraud-screen, cost and brief creators for a paid influencer campaign, then benchmark it with EMV and lock disclosure compliance
- **`media-strategy`** — Build a media / communications-channel strategy: audience-first channel selection, role of each channel against the funnel, reach vs frequen
- **`persona-population-builder`** — Build a large, statistically-grounded synthetic persona population by scaffolding on PersonaHub (proj-persona/PersonaHub, 200K preview / 1B
- **`qualitative-research`** — Run the qualitative research lifecycle — design, fieldwork prep, analysis, synthesis
- **`raw-data-research`** — Write and execute scripts to parse, clean, normalise and triangulate large volumes of raw data — messy CSVs, multi-tab XLSX exports, JSON du
- **`retail-media-network-strategy`** — Build a retail media network (RMN) plan: split budget across Amazon Ads, Walmart Connect, Kroger, Instacart and others by marginal increment
- **`share-of-search`** — Compute, interpret, and present share of search (SoS) as a leading indicator of brand health and a competitive lens — the Les Binet / James
- **`social-listening-orchestration`** — Run a full social-listening study end to end over Brand24 or Brandwatch — not just a raw connector pull
- **`strategy-analyst`** — Acts as a strategic analyst across media and advertising — the hybrid role that takes numbers (brand tracker, MMM, search, social listening
- **`synthetic-audience-message-testing`** — Pre-flight ad copy, taglines, value props, or full creative against a simulated target audience BEFORE spend, and return a decision-ready re
- **`synthetic-focus-group`** — Run a synthetic focus group, brainstorm or concept-reaction session with Microsoft TinyTroupe LLM personas for fast, cheap, directional reac
- **`systematic-creative-variant-matrix`** — Turn a creative brief into 50+ ad variants laid out as a hypothesis-indexed matrix — value-prop x avatar x hook x format x style — where eve
- **`trend-foresight`** — Identify, weight, and act on trends — separating fads, micro- shifts, macro-shifts, and structural changes — and write a foresight POV with

## ui-effects · 22

- **`animated-counter`** — Number that counts up (or down) to a target value when it scrolls into view
- **`aurora-gradient`** — Animated multi-color gradient blob background (React, pure CSS) — soft blurred "aurora" blobs drift across a container
- **`bento-grid`** — Responsive bento-box grid layout for React — variable column/row spans per card, with 3D hover tilt + lift that tracks the cursor
- **`css-scroll-driven-animations`** — Build scroll-linked and view-triggered animations with native CSS — zero JavaScript, zero IntersectionObserver — using animation-timeline: s
- **`floating-label-input`** — Material/Stripe-style floating label input for React — label sits inside the field at rest, floats up and shrinks when focused or filled, wi
- **`framer-level-interactions`** — Build Framer-grade interactive section components for marketing pages, product sites, portfolios, and dashboards
- **`image-shatter`** — Build a React/Next.js component that shatters an image into a grid of tiles on hover, with each tile flying outward/rotating with spring phy
- **`infinite-marquee`** — Seamless infinite horizontal marquee / logo ticker in React using pure CSS animation — duplicates children, scrolls forever, pauses on hover
- **`interactive-distortion`** — Build a WebGL2 interactive mouse-driven pixel distortion effect for images and videos in React/Next.js
- **`liquid-glass-button`** — Build an Apple-style liquid glass back button (or generic glass button) in React/Next.js using pure CSS — no WebGL, no canvas
- **`liquid-image`** — Build a WebGL liquid-water hover effect for images in React/Next.js — ripples follow the cursor, and a grayscale-to-colour reveal mask follo
- **`magnetic-button`** — React button that attracts toward the cursor when it's within a radius, with spring-damped follow and elastic snap-back on exit
- **`magnetic-cursor`** — Custom cursor dot that follows the mouse with spring lag, scales up over interactive elements, shrinks on mousedown, and uses mix-blend-mode
- **`rive-lottie-web-animation`** — Embed a designer-authored Rive (.riv) state machine or dotLottie (.lottie) animation at runtime on the web and DRIVE it from code — fire tri
- **`rubiks-image-cube`** — Build an interactive 3D Rubik's cube component in React/Next.js using CSS 3D transforms and framer-motion
- **`scroll-reveal-section`** — React wrapper that fades and slides its children into view with stagger as the section enters the viewport
- **`skeleton-and-optimistic-ui`** — Build perceived-speed UI: shimmer/skeleton placeholders while data loads, React Suspense streaming with the use() hook, and instant optimist
- **`spectra-noise`** — Build a WebGL shader-based animated noise background component for React/Next.js
- **`spectral-distortion`** — Compose a red-themed spectra-noise WebGL shader background with an interactive-distortion image overlay so that the spectra's animated field
- **`text-scramble`** — Text that scrambles random glyphs and then "decrypts" into its final string when triggered (viewport/hover/mount)
- **`theme-toggle`** — Animated sun/moon theme toggle button for React — smoothly morphs between a sunburst and a crescent moon, persists the choice to localStorag
- **`view-transitions-morphing`** — Use the native View Transitions API to morph between UI states and pages with zero animation JS — crossfade the whole viewport, or FLIP-morp

## verification · 14

- **`adversarial-argument-review`** — Steelman an argument then attack it — reconstruct its strongest form, surface hidden assumptions, name logical fallacies, build the stronges
- **`ai-provenance-deepfake-check`** — Verify whether an image, video, or audio file is AI-generated, edited, or authentic by reading C2PA / Content Credentials manifests, checkin
- **`citation-integrity-check`** — Verify every reference in a manuscript, report, or LLM output actually EXISTS and says what it is cited for
- **`claim-verifier`** — Fact-check prose by decomposing it into atomic, decontextualized, checkable claims, retrieving evidence per claim (web/corpus), and returnin
- **`eval-dataset-curation`** — Build, version and defend a golden eval set BEFORE trusting any eval number — pin an immutable frozen slice with a content hash, run a two-t
- **`experiment-validity-audit`** — Gate an A/B test result before anyone calls a winner — run the four integrity checks that catch most false positives: Sample Ratio Mismatch
- **`inter-annotator-agreement`** — Measure how much your human labellers (or LLM judges) actually agree, using Cohen's kappa, Fleiss' kappa, and Krippendorff's alpha — then ro
- **`llm-judge-bias-audit`** — Audit and debias an LLM-as-judge before you trust its scores
- **`online-eval-drift-monitor`** — Stand up online evaluation on live LLM traffic: sample a slice of production traces, score each with an LLM-judge or code scorer, compare th
- **`reproducible-insight-workbench`** — Bind every headline stat and chart in a deck, report or pitch to the EXACT dataset + query/code that produced it, then run a reviewer pass t
- **`research-methodology-review`** — Appraise the METHODOLOGY behind a study, survey or research claim before you trust or cite it — match it to the right reporting guideline (C
- **`self-consistency-check`** — Catch likely LLM hallucinations with NO knowledge base — re-sample the same answer N times at high temperature and flag any fact that change
- **`source-credibility-audit`** — Grade the sources behind a claim, deck, or report — reliability track record, independence and conflicts of interest, primary-vs-secondary
- **`stat-check-review`** — Recompute and stress-test the statistics in a report, deck or paper so a wrong number never ships — statcheck-style t/F/χ²/df/p consistency

## voice-agents · 1

- **`realtime-voice-agent`** — Build a low-latency speech-to-speech conversational voice agent with barge-in, natural turn-taking, and mid-call tool/function calls
